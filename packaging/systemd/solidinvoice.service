[Unit]
Description=SolidInvoice - Simple and elegant invoicing solution
Documentation=https://solidinvoice.co/docs
After=network-online.target
Wants=network-online.target

[Service]
Type=exec
User=augias
Group=augias

ExecStart=/usr/bin/augias run

EnvironmentFile=-/etc/augias/augias.env

WorkingDirectory=/var/lib/augias

Restart=on-failure
RestartSec=5
TimeoutStartSec=30
TimeoutStopSec=30

# Security hardening
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
PrivateDevices=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
RestrictNamespaces=true
LockPersonality=true
RemoveIPC=true

ReadWritePaths=/etc/augias /var/lib/augias

# Allow binding to privileged ports if needed
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=augias

[Install]
WantedBy=multi-user.target

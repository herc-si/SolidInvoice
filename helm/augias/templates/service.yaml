apiVersion: v1
kind: Service
metadata:
  name: {{ include "augias.fullname" . }}
  labels:
    {{- include "augias.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "augias.selectorLabels" . | nindent 4 }}
